(package space
  
  (root "src")
  (products (space (title "Space")
                   (run space.run)
                   (unit space.product)
                   (build (space executable: "space" resources: "resources"))
                   (update space.biome
                           space.client
                           space.product
                           space.run
                           space.window
                           space.windows
                           space.world
                           space.zone)
                   (dependencies world
                                 world.client
                                 world.server)))
  (project ".project"))
