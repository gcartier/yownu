(package space
  
  (root "src")
  (products (space (title "Space")
                   (run space.run)
                   (unit space.product)
                   (build (space executable: "Space" resources: "resources"))
                   (update space.biome
                           space.client
                           space.product
                           space.run
                           space.start
                           space.window
                           space.windows
                           space.world
                           space.zone)
                   (dependencies jazz.git
                                 jazz.groupware
                                 world
                                 world.client
                                 world.server)))
  (project ".project"))
